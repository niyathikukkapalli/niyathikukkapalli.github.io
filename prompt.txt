I want to update my artwork gallery page to display images as cards instead of the current layout. Each card should have:

1. The artwork image at the top
2. A white background with rounded corners (8px border-radius)
3. A subtle shadow (box-shadow: 0 2px 8px rgba(0,0,0,0.1))
4. Padding of 15px below the image for text
5. Card title in bold, 16px font
6. Medium/description text in gray (#777), 14px font
7. A hover effect that lifts the card up slightly (translateY(-5px)) and increases the shadow (0 8px 20px rgba(0,0,0,0.15))
8. Smooth transitions (0.3s ease) for the hover effects

The cards should be displayed in a masonry/Pinterest-style layout using CSS columns:
- 3 columns on desktop (column-count: 3)
- 2 columns on tablets (max-width: 900px)
- 1 column on mobile (max-width: 600px)
- 20px gap between columns and cards

Keep all my existing header, navigation, and page title styling exactly as is. Only update the gallery/artwork display section.